"""DS-Extender generated renderers land here.

Each file must expose a top-level ``render(body, content)`` function and
may only import from ``..xml_utils``, ``..tokens``, ``docx.*``, and ``re``.
See ``render.dynamic_dispatch`` for the AST validator.
"""
